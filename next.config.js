/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove @cloudflare/next-on-pages static configuration for local development
  // This allows API routes to work properly during development
}

module.exports = nextConfig